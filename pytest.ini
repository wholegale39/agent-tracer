[pytest]
# e2e tests that need a live server are excluded from CI unit test run;
# test_e2e_cost.py is in-process (TestClient + temp DB) so it runs in CI.
testpaths = test_regression.py test_export.py test_cost.py test_e2e_cost.py
